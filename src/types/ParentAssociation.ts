export interface ParentAssociation {
    id: number;
    firstName: string;
    lastName: string;
    parentid: number;
    // studentId: number;
  }

export interface Count{
  id: number;
  firstName: string;
  lastName: string;
  padres: number;
}